# import '../../pokemon_card/graphql/pokemon_card_fragment.graphql'

query PokemonDetail($input: pokemonInput) {
  pokemon(input: $input) {
    ...PokemonCard
    weight {
      minimum
      maximum
    }
    height {
      minimum
      maximum
    }
  }
}
